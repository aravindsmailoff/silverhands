[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd video-backend && pip install -r requirements.txt && uvicorn app:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/docs"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
